#include<stdio.h>
int main()
{
	int marks;
	printf("Enter yours marks: ");
	scanf("%d", &marks);
	/**if(marks >= 80)
		printf("The equivalent gradde is: A\n");
	else if(marks >=75 && marks <80)
		printf("The equivalent grade is: A-\n");
	else if(marks >=70 && marks < 75)
		printf("The grade point is: B+\n");
	else if(marks >= 65 && marks < 70)
		printf("The grade point is: B\n");
	else if(marks >= 60 && marks < 65)
		printf("The grade point is: B-\n");
	else if(marks >= 55 && marks < 60)
		printf("The grade point is: C+\n");
	else if(marks >= 50 && marks < 55)
		printf("The grade point is: C\n");
	else if(marks >= 45 && marks < 50)
		printf("The grade point is: C-\n");
	else if(marks >= 40 && marks < 45)
		printf("The grade point is: D\n");
	else if(marks < 40)
		printf("The grade point is: F\n");
		**/
		if(marks >= 80)
		printf("The equivalent gradde is: A\n");
	 if(marks >=75 && marks <80)
		printf("The equivalent grade is: A-\n");
	 if(marks >=70 && marks < 75)
		printf("The grade point is: B+\n");
	if(marks >= 65 && marks < 70)
		printf("The grade point is: B\n");
	if(marks >= 60 && marks < 65)
		printf("The grade point is: B-\n");
	 if(marks >= 55 && marks < 60)
		printf("The grade point is: C+\n");
	 if(marks >= 50 && marks < 55)
		printf("The grade point is: C\n");
	 if(marks >= 45 && marks < 50)
		printf("The grade point is: C-\n");
	 if(marks >= 40 && marks < 45)
		printf("The grade point is: D\n");
	if(marks < 40)
		printf("The grade point is: F\n");
	return 0;
}
